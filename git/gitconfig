[color]
  ui = true

[commit]
  gpgsign = true

[core]
  editor = vim
  excludesfile = ~/.dotfiles/git/ignore

[merge]
  tool = opendiff

[merge "npm-merge-driver"]
  driver = npx npm-merge-driver merge %A %O %B %P
  name = automatically merge npm lockfiles

[gpg]
  program = gpg

[ghi]
  token = !security find-internet-password -a johndjameson -s github.com -l 'ghi token' -w

[github]
  user = johndjameson

[push]
  default = simple

[user]
  email = thejohnjameson@gmail.com
  name = John D. Jameson
  signingkey = 1C0F1401FE5DD8C4
